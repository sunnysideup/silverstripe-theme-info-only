export function dynamicButtonInit() {
    // const subsections = document.getElementsByClassName('site-sub-section');
    // for (var i = 0; i < subsections.length; i++) {
    //     const subsection = subsections[i];
    //     subsection.removeEventListener('mouseenter', enterEventHandler, true);
    //     subsection.addEventListener('mouseenter', enterEventHandler, true);
    //     subsection.removeEventListener('mouseout', exitEventHandler, true);
    //     subsection.addEventListener('mouseout', exitEventHandler, true);
    // }
}

function enterEventHandler(event) {
    // event.currentTarget.classList.add("hovered-section");
}

function exitEventHandler(event) {
    // event.currentTarget.classList.remove("hovered-section");
}